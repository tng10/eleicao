{% extends '::base.html.twig' %}

{% block body %}
    <h1>Nova Proposta</h1>

    <form class="form-horizontal" action="{{ path('proposta_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form, {'form_type': 'horizontal'}) }}
        <div class="control-group">
            <div class="form-actions">
                <button type="submit" class="btn btn-primary">Cadastrar</button>
                <a class="btn btn-info" href="{{ path('proposta') }}">
                    Listar registros
                </a>
            </div>
        </div>
    </form>

{% endblock %}

