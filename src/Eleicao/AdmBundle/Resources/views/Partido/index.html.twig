{% extends '::base.html.twig' %}

{% block body %}
    <h1>Partidos</h1>

    {% if entities %}
    <table class="records_list table table-condensed table-hover">
        <thead>
            <tr>
                <th>Sigla</th>
                <th>Nome</th>
                <th>Ações</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td>{{ entity.sigla }}</td>
                <td>{{ entity.nome }}</td>
                <td>
                    <a class="btn btn-info" href="{{ path('partido_show', { 'id': entity.id }) }}">exibir</a>
                    <a class="btn btn-info" href="{{ path('partido_edit', { 'id': entity.id }) }}">editar</a>                
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    {% else %}
        <div class="no-records-msg">
            Ainda não existem registros
        </div>
    {% endif %}

    <a class="btn btn-success" href="{{ path('partido_new') }}">
        Novo registro
    </a>
{% endblock %}
